[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mirrorus"
version = "0.1"
dependencies = [
    "streamlit",
    "mediapipe",
    "opencv-python",
    "numpy"
]

[tool.setuptools]
packages = ["src"]
